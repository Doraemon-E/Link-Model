services:
  nginx:
    image: nginx:stable-alpine
    container_name: link-model-nginx
    ports:
      - "8080:80"
    volumes:
      - .:/usr/share/nginx/html:ro
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
